class LabyrinthGenerator {
    constructor(rows, cols, start, goal, maze) {
        this.rows = rows;
        this.cols = cols;
        this.start = start;
        this.goal = goal;
        this.maze = maze;
    }
}